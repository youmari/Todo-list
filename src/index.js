import listData from "./js/data.js";

function listObject(description,completed,number) {
  this.description = description;
  this.completed = completed;
  this.number = number;
}

const createElements = () => {
  

}